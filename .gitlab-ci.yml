stages:
    - npm
    - test

npm:
    stage: npm
    tags:
        - type-inference
    script:
        - npm install
    cache:
        paths:
            - node_modules/
    artifacts:
        expire_in: 1 days
        when: on_success
        paths:
            - node_modules/

test:
    stage: test
    tags:
        - type-inference
    dependencies:
        - npm
    script:
        - npm test